# StudioNet integration tests

This directory is intentionally a release gate rather than a mocked proof. A live deployment and injected-wallet transactions are required before StudioNet evidence can be recorded. No local/server private key path is permitted.
